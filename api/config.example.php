<?php
// Copy this file to config.php and paste your real Groq API key.
// config.php is listed in .gitignore, so the key is never committed.
// Alternative: set the GROQ_API_KEY environment variable instead of using this file.

define('GROQ_API_KEY', 'gsk_your_key_here');

// While developing, set this to true to see the real error message in the chat
// widget when something goes wrong (it is always logged either way).
// Keep it off or remove it in production so visitors don't see internal details.
// define('APP_DEBUG', true);
